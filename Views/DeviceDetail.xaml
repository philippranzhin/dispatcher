<UserControl x:Class="DispatcherDesktop.Views.DeviceDetail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DispatcherDesktop.Views"
             xmlns:mvvm="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="150" d:DesignWidth="200">
    <DockPanel VerticalAlignment="Stretch">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" 
                       VerticalAlignment="Center" 
                       Margin="5"
                       Style="{DynamicResource MaterialDesignTitleTextBlock}">
                    <Run>Информация об устройстве</Run> 
                    <Run Foreground="{DynamicResource PrimaryHueDarkBrush}" Text="{Binding DeviceData.Name, Mode=OneWay}"/>
            </TextBlock>
            <WrapPanel>
                <materialDesign:Card Width="200" Height="150" Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Адрес устройства в сети</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                        />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.Address, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                <materialDesign:Card Width="200" Height="150" Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Температура наружного воздуха</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                                   />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.OutdoorTemperature, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/> <Run Text="&#186;"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                <materialDesign:Card Width="200" Height="150"  Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Температура отопления, подача</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                        />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.DirectTemperature, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/> <Run Text="&#186;"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                <materialDesign:Card Width="200" Height="150"  Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Температура смешения</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                        />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.MixingTemperature, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/> <Run Text="&#186;"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                <materialDesign:Card Width="200" Height="150"  Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Температура отопления, обратка</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                        />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.ReturnTemperature, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/> <Run Text="&#186;"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                <materialDesign:Card Width="200" Height="150"  Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Задание отопления</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                        />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.GivingHeatingTemperature, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/> <Run Text="&#186;"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                            <materialDesign:Card Width="200" Height="150" Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Адрес устройства в сети</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                        />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.Address, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                <materialDesign:Card Width="200" Height="150" Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Температура наружного воздуха</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                                   />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.OutdoorTemperature, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/> <Run Text="&#186;"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                <materialDesign:Card Width="200" Height="150"  Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Температура отопления, подача</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                        />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.DirectTemperature, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/> <Run Text="&#186;"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                <materialDesign:Card Width="200" Height="150"  Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Температура смешения</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                        />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.MixingTemperature, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/> <Run Text="&#186;"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                <materialDesign:Card Width="200" Height="150"  Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Температура отопления, обратка</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                        />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.ReturnTemperature, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/> <Run Text="&#186;"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                <materialDesign:Card Width="200" Height="150"  Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DockPanel>
                        <materialDesign:ColorZone DockPanel.Dock="Top" Padding="10">
                            <DockPanel>
                                <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges">
                                    <ListBox>
                                        <ListBoxItem>Записать новое значение</ListBoxItem>
                                    </ListBox>
                                </materialDesign:PopupBox>
                                <TextBlock DockPanel.Dock="Left" TextWrapping="WrapWithOverflow" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" LineStackingStrategy="MaxHeight">Задание отопления</TextBlock>
                            </DockPanel>
                        </materialDesign:ColorZone>
                        <TextBlock DockPanel.Dock="Bottom" 
                                   TextWrapping="WrapWithOverflow" 
                                   Style="{DynamicResource MaterialDesignBody1TextBlock}" 
                                   Foreground="{DynamicResource PrimaryHueDarkBrush}"
                                   Padding="2"
                                   HorizontalAlignment="Right"
                                   LineStackingStrategy="MaxHeight" 
                                   Text="{Binding DeviceData.Date}"
                        />
                        <TextBlock Style="{DynamicResource MaterialDesignTitleTextBlock}" Padding="10" Margin="5">
                            <Run Text="{Binding DeviceData.GivingHeatingTemperature, Mode=OneWay}" Foreground="{DynamicResource SecondaryAccentBrush}"/> <Run Text="&#186;"/>
                        </TextBlock>
                    </DockPanel>
                </materialDesign:Card>
                </WrapPanel>
        </DockPanel>
    </DockPanel>
</UserControl>
